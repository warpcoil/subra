all:
	ARCH=$(ARCH) PREFIX32=$(PREFIX32) PREFIX64=$(PREFIX64) EXTRAFLAGS=$(EXTRAFLAGS) make -C x16 all
	ARCH=$(ARCH) PREFIX32=$(PREFIX32) PREFIX64=$(PREFIX64) EXTRAFLAGS=$(EXTRAFLAGS) make -C libgcc all
	ARCH=$(ARCH) PREFIX32=$(PREFIX32) PREFIX64=$(PREFIX64) EXTRAFLAGS=$(EXTRAFLAGS) make -C oslib all
	ARCH=$(ARCH) PREFIX32=$(PREFIX32) PREFIX64=$(PREFIX64) EXTRAFLAGS=$(EXTRAFLAGS) make -C libm_core all
	ARCH=$(ARCH) PREFIX32=$(PREFIX32) PREFIX64=$(PREFIX64) EXTRAFLAGS=$(EXTRAFLAGS) make -C miniz all
	ARCH=$(ARCH) PREFIX32=$(PREFIX32) PREFIX64=$(PREFIX64) EXTRAFLAGS=$(EXTRAFLAGS) make -C mujs all
	ARCH=$(ARCH) PREFIX32=$(PREFIX32) PREFIX64=$(PREFIX64) EXTRAFLAGS=$(EXTRAFLAGS) make -C my all
	ARCH=$(ARCH) PREFIX32=$(PREFIX32) PREFIX64=$(PREFIX64) EXTRAFLAGS=$(EXTRAFLAGS) make -C x32 all

